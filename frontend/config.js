// This is client side config only - don't put anything in here that shouldn't be public!
export const endpoint = `http://localhost:4402`
export const prodEndpoint = `http://screens-yoga.jimorluk.com/` //Update this to endpoint when deploying
